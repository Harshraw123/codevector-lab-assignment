# Backend API URL (no trailing slash).
# Production: https://codevector-lab-assignment.onrender.com
# Local dev: leave empty to use the Vite /api proxy (see vite.config.js).
VITE_API_URL=https://codevector-lab-assignment.onrender.com
